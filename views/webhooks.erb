<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="panel panel-default">
      <!-- Default panel contents -->
      <div class="panel-heading">
        <a class="btn btn-default btn-xs pull-right"  href="<%= url '/webhooks/new' %>">Add webhook</a>
        <b>Webhooks</b>
      </div>
      <div class="panel-body">
        <p>Webhooks allow external services to be notified when certain events happen within EveryPolitician. When events happen, we’ll send a POST request to each of the URLs you provide.</p>
      </div>

      <!-- List group -->
      <ul class="list-group">
        <% @applications.each do |application| %>
          <li class="list-group-item">
            <a class="btn btn-default btn-xs pull-right" href="<%= url "/webhooks/#{application.id}" %>"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
            <a href="<%= url "/webhooks/#{application.id}" %>"><%= application.webhook_url %></a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
